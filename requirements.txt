# Core
torch>=2.2.0
transformers>=4.40.0
huggingface-hub>=0.22.0
safetensors>=0.4.0
datasets>=2.19.0
numpy>=1.24.0
tqdm>=4.66.0

# Apple Silicon training backend (optional elsewhere; required to run the sweep)
mlx>=0.32.0 ; sys_platform == "darwin" and platform_machine == "arm64"

# Tests
pytest>=8.0.0
